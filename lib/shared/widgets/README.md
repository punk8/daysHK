# lib/shared/widgets

## 职责

存放通用 UI 组件。

## 建议组件

- `StatusCard`
- `MetricCard`
- `PermissionWarningCard`
- `RecordTimelineItem`
- `SourceBadge`
- `ConfirmationBadge`
- `PrimaryActionButton`
- `EmptyState`
- `ConfirmDialog`

## 原则

- 组件应服务当前 UI 示意图，不要一开始做过度通用的设计系统。
- 文案应从调用方传入，避免组件里硬编码业务结论。

