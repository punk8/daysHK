# assets

## 职责

存放 App 运行时需要打包的静态资源。

建议结构：

- `geo/`：香港边界数据、bbox、版本说明。
- `images/`：App 图标、空状态图、参考图片等。

## 注意事项

- 边界数据必须保留来源、下载日期、许可说明。
- 不应把临时设计稿、未压缩大文件或来源不明的数据直接打包进 App。
- Flutter 项目创建后，需要在 `pubspec.yaml` 中声明需要打包的 assets。

