# ios/DaysInHkWidget

iOS WidgetKit extension for the Days in HK home screen widget.

- Reads summary values from App Group `group.com.punk8.daysHK`.
- Supports `systemSmall` and `systemMedium`.
- Shows an empty state when no summary has been written yet.
