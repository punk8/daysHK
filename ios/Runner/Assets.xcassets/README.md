# ios/Runner/Assets.xcassets

## 职责

iOS 原生资源目录。

当前包含：

- `AppIcon.appiconset/`：App 图标。
- `LaunchImage.imageset/`：启动图资源。

## 注意事项

- Flutter 页面内图片资源优先放在 `assets/images/`。
- iOS App 图标需要覆盖 App Store 和设备要求的所有尺寸。
- 替换图标后应在真机或模拟器确认启动屏和桌面图标显示正常。

